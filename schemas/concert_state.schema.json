{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ConcertState",
  "description": "Current state of scraped concert data. Written by Cowork, read by frontend and diff logic.",
  "type": "object",
  "required": ["last_run", "center", "artist_shows", "venue_shows"],
  "properties": {
    "last_run": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Date of last scrape run (YYYY-MM-DD)"
    },
    "center": {
      "type": "string",
      "description": "Map home city, e.g. 'Des Moines, IA'"
    },
    "radius_miles": {
      "type": ["number", "null"],
      "description": "Deprecated — kept for backward compat, no longer used for filtering"
    },
    "artist_shows": {
      "type": "object",
      "description": "Map of artist name to array of all North America shows",
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "#/definitions/Show" }
      }
    },
    "venue_shows": {
      "type": "object",
      "description": "Map of venue name to array of events at that venue",
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "#/definitions/VenueShow" }
      }
    }
  },
  "definitions": {
    "Show": {
      "type": "object",
      "required": ["date", "venue", "city", "status"],
      "properties": {
        "date": {
          "type": "string",
          "description": "Display date, e.g. 'Mar 15, 2026'"
        },
        "venue": {
          "type": "string",
          "description": "Venue name"
        },
        "city": {
          "type": "string",
          "description": "City and state, e.g. 'Kansas City, MO'"
        },
        "status": {
          "type": "string",
          "enum": ["on_sale", "sold_out"]
        },
        "lat": {
          "type": ["number", "null"],
          "description": "Venue latitude"
        },
        "lon": {
          "type": ["number", "null"],
          "description": "Venue longitude"
        }
      }
    },
    "VenueShow": {
      "type": "object",
      "required": ["date", "artist", "tracked"],
      "properties": {
        "date": {
          "type": "string",
          "description": "Display date"
        },
        "artist": {
          "type": "string",
          "description": "Artist or event name"
        },
        "tracked": {
          "type": "boolean",
          "description": "True if artist matches a tracked artist in config"
        }
      }
    }
  }
}
