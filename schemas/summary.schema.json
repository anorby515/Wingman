{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Summary",
  "description": "Public summary for GitHub Pages. Generated by the daily TM data fetch.",
  "type": "object",
  "required": ["generated_at", "center", "center_lat", "center_lon", "artist_shows", "venue_shows", "changes"],
  "properties": {
    "generated_at": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Date this summary was generated (YYYY-MM-DD)"
    },
    "center": {
      "type": "string",
      "description": "Map home city name"
    },
    "center_lat": {
      "type": "number",
      "description": "Latitude of center city (for map rendering)"
    },
    "center_lon": {
      "type": "number",
      "description": "Longitude of center city (for map rendering)"
    },
    "artist_shows": {
      "type": "object",
      "description": "Map of artist name to all North America shows",
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "#/definitions/SummaryShow" }
      }
    },
    "venue_shows": {
      "type": "object",
      "description": "Map of venue name to events at that venue",
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "#/definitions/SummaryVenueShow" }
      }
    },
    "festival_shows": {
      "type": "object",
      "description": "Map of festival name to events for that festival",
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "#/definitions/SummaryFestivalShow" }
      }
    },
    "festivals_not_found": {
      "type": "array",
      "description": "Festivals that returned zero matching results on Ticketmaster",
      "items": { "type": "string" }
    },
    "festival_coming_soon": {
      "type": "array",
      "description": "Festival events not yet on public sale",
      "items": { "$ref": "#/definitions/ComingSoonFestivalEvent" }
    },
    "changes": {
      "type": "object",
      "description": "Diff from previous run",
      "properties": {
        "artists": {
          "type": "object",
          "additionalProperties": { "$ref": "#/definitions/ArtistChanges" }
        },
        "venues": {
          "type": "object",
          "additionalProperties": { "$ref": "#/definitions/VenueChanges" }
        },
        "total_added": { "type": "number" },
        "total_removed": { "type": "number" },
        "total_sold_out": { "type": "number" }
      }
    },
    "coming_soon": {
      "type": "array",
      "description": "Shows not yet on public sale, sourced from Ticketmaster Discovery API.",
      "items": { "$ref": "#/definitions/ComingSoonShow" }
    },
    "coming_soon_fetched": {
      "type": ["string", "null"],
      "description": "ISO 8601 datetime when coming_soon was last fetched from Ticketmaster"
    }
  },
  "definitions": {
    "SummaryShow": {
      "type": "object",
      "required": ["date", "venue", "city", "status"],
      "properties": {
        "date": { "type": "string" },
        "raw_date": { "type": "string", "description": "ISO 8601 date for sorting (YYYY-MM-DD)" },
        "venue": { "type": "string" },
        "city": { "type": "string" },
        "status": { "enum": ["on_sale", "sold_out", "coming_soon"] },
        "lat": { "type": ["number", "null"] },
        "lon": { "type": ["number", "null"] },
        "is_new": { "type": "boolean", "description": "True if this show was added in the latest run" },
        "onsale_datetime": { "type": ["string", "null"], "description": "ISO 8601 public on-sale start (only when status is coming_soon)" },
        "onsale_tbd": { "type": "boolean", "description": "True if on-sale date is TBD" },
        "ticketmaster_url": { "type": "string", "description": "Direct link to Ticketmaster event page" }
      }
    },
    "SummaryVenueShow": {
      "type": "object",
      "required": ["date", "artist", "tracked"],
      "properties": {
        "date": { "type": "string" },
        "artist": { "type": "string" },
        "tracked": { "type": "boolean" }
      }
    },
    "SummaryFestivalShow": {
      "type": "object",
      "required": ["date", "venue", "city", "event_name"],
      "properties": {
        "date": { "type": "string" },
        "raw_date": { "type": "string", "description": "ISO 8601 date for sorting (YYYY-MM-DD)" },
        "venue": { "type": "string" },
        "city": { "type": "string" },
        "event_name": { "type": "string", "description": "TM event name (may differ from tracked festival name)" },
        "status": { "enum": ["on_sale", "sold_out", "coming_soon"] },
        "lat": { "type": ["number", "null"] },
        "lon": { "type": ["number", "null"] },
        "is_new": { "type": "boolean", "description": "True if this show was added in the latest run" },
        "onsale_datetime": { "type": ["string", "null"], "description": "ISO 8601 public on-sale start (only when status is coming_soon)" },
        "onsale_tbd": { "type": "boolean", "description": "True if on-sale date is TBD" },
        "ticketmaster_url": { "type": "string", "description": "Direct link to Ticketmaster event page" }
      }
    },
    "ComingSoonFestivalEvent": {
      "type": "object",
      "required": ["tracked_festival", "event_name", "date", "venue", "city"],
      "properties": {
        "tracked_festival": { "type": "string", "description": "Festival name as stored in config" },
        "event_name": { "type": "string", "description": "TM event name (may include tier/pass info)" },
        "date": { "type": "string", "description": "Display date, e.g. 'Aug 15, 2026'" },
        "venue": { "type": "string" },
        "city": { "type": "string" },
        "onsale_datetime": { "type": ["string", "null"], "description": "ISO 8601 public on-sale start" },
        "onsale_tbd": { "type": "boolean" },
        "presales": {
          "type": "array",
          "items": { "$ref": "#/definitions/ComingSoonPresale" }
        },
        "ticketmaster_url": { "type": "string" },
        "lat": { "type": ["number", "null"] },
        "lon": { "type": ["number", "null"] }
      }
    },
    "ArtistChanges": {
      "type": "object",
      "properties": {
        "added": { "type": "array", "items": { "$ref": "#/definitions/SummaryShow" } },
        "removed": { "type": "array", "items": { "$ref": "#/definitions/SummaryShow" } },
        "newly_sold": { "type": "array", "items": { "$ref": "#/definitions/SummaryShow" } }
      }
    },
    "VenueChanges": {
      "type": "object",
      "properties": {
        "added": { "type": "array", "items": { "$ref": "#/definitions/SummaryVenueShow" } },
        "removed": { "type": "array", "items": { "$ref": "#/definitions/SummaryVenueShow" } }
      }
    },
    "ComingSoonPresale": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": { "type": "string", "description": "Presale name, e.g. 'Fan Club Presale'" },
        "start_datetime": { "type": ["string", "null"], "description": "ISO 8601 presale start" },
        "end_datetime": { "type": ["string", "null"], "description": "ISO 8601 presale end" }
      }
    },
    "ComingSoonShow": {
      "type": "object",
      "required": ["artist", "date", "venue", "city", "ticketmaster_url"],
      "properties": {
        "artist": { "type": "string" },
        "genre": { "type": "string" },
        "date": { "type": "string", "description": "Display date, e.g. 'Aug 15, 2026'" },
        "venue": { "type": "string" },
        "city": { "type": "string" },
        "onsale_datetime": { "type": ["string", "null"], "description": "ISO 8601 public on-sale start" },
        "onsale_tbd": { "type": "boolean" },
        "presales": {
          "type": "array",
          "items": { "$ref": "#/definitions/ComingSoonPresale" }
        },
        "ticketmaster_url": { "type": "string" },
        "lat": { "type": ["number", "null"] },
        "lon": { "type": ["number", "null"] }
      }
    }
  }
}
